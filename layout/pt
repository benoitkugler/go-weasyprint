    if markerBoxes && len(box.children) == 1 {
        // See https://www.w3.org/TR/css-lists-3/#list-style-position-outside
        #
        // "The size || contents of the marker box may affect the height of the
        //  principal block box and/or the height of its first line box, && in
        //  some cases may cause the creation of a new line box; this
        //  interaction is also not defined."
        #
        // We decide here to add a zero-width space to have a minimum
        // height. Adding text boxes is not the best idea, but it"s not a good
        // moment to add an empty line box, && the specification lets us do
        // almost what we want, so…
        if style["listStylePosition"] == "outside" {
            box.children.append(boxes.TextBox.anonymousFrom(box, "​"))
